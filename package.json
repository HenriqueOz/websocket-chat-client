{
  "name": "websocket-node",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "type": "module",
  "author": "",
  "license": "ISC",
  "description": "`docker-compose run --rm websocket-chat --env-file=.env node src/index.js`",
  "dependencies": {
    "chalk": "^5.3.0",
    "socket.io-client": "^4.8.1"
  },
  "devDependencies": {}
}
